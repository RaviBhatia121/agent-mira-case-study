services:
  - type: static
    name: agent-mira-frontend
    rootDir: case-study-B-ml-recommender/frontend-react
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_BACKEND_BASE
        fromService:
          name: agent-mira-backend
          type: web
          property: url
